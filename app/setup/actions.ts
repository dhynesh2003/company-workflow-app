"use server";import { createClient } from "@/lib/supabase/server";import { redirect } from "next/navigation";export async function bootstrap(formData:FormData){const supabase=await createClient();const {error}=await supabase.rpc("bootstrap_first_admin",{p_org_name:String(formData.get("organization")),p_full_name:String(formData.get("full_name"))});if(error)redirect(`/setup?error=${encodeURIComponent(error.message)}`);redirect("/dashboard")}