import { login } from "./actions";export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="auth"><section className="card authbox"><h1>Company WorkFlow</h1><p className="muted">Sign in to manage and complete assigned work.</p>{p.error&&<p className="error">{p.error}</p>}<form action={login} className="form"><label className="label">Email<input className="input" name="email" type="email" required/></label><label className="label">Password<input className="input" name="password" type="password" required minLength={6}/></label><button className="btn">Sign in</button></form></section></main>}